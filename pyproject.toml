[project]
name = "solpy"
version = "0.1.0"
description = "Python SDK for Solana"
authors = [{ name = "SolPy Team" }]
requires-python = ">=3.10"

dependencies = []

[tool.ruff]
line-length = 100

[tool.mypy]
strict = true